---
name: stack-enable
description: Enable a `reference`, `skill`, or `agent` by updating the canonical `stack.yml` source of truth.
argument-hint: "[type] [artifact] [scope?]"
user-invocable: true
---

# Stack Enable

GitHub Copilot adapter for [`../../../skills/stack-enable/SKILL.md`](../../../skills/stack-enable/SKILL.md).

Use this skill to activate one supported artifact in `stack.yml`, with root-first precedence in monorepos.
